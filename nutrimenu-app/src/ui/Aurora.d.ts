import * as React from 'react';
import { ViewStyle, StyleProp } from 'react-native';

/** Мягкое свечение на фоне. Нативно — сетчатый градиент, в вебе — линейный. */
export const Aurora: React.FC<{ style?: StyleProp<ViewStyle> }>;
