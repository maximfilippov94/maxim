<?php
/**
 * Глобальная конфигурация приложения.
 * Значения можно переопределить через переменные окружения.
 */

return [
    // Путь к файлу БД SQLite. На проде можно указать абсолютный путь.
    'db_path' => getenv('NUTRI_DB_PATH') ?: __DIR__ . '/data/app.sqlite',

    // Каталог с нумерованными миграциями .sql
    'migrations_dir' => __DIR__ . '/migrations',

    // Срок жизни токена сессии (в секундах). По умолчанию 30 дней.
    'session_ttl' => (int)(getenv('NUTRI_SESSION_TTL') ?: 60 * 60 * 24 * 30),

    // Разрешённые типы приёмов пищи (порядок важен — по нему сортируется день)
    'meal_types' => ['breakfast', 'snack1', 'lunch', 'snack2', 'dinner'],

    // Окружение: dev | prod. В dev отдаются подробные ошибки.
    'env' => getenv('NUTRI_ENV') ?: 'dev',
];
